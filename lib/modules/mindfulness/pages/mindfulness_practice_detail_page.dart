import 'package:flutter/material.dart';

class MindfulnessPracticeDetailPage extends StatelessWidget {
  const MindfulnessPracticeDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Mindfulness Practice Detail (TODO)')));
  }
}
